```{r prep, echo = FALSE, results = "hide", cache = FALSE}
library(exams)

## Generate some data
a <- sample(1:5, 3)
b <- sample(1:5, 3)

solution <- paste(as.character(a*b), collapse = ",")
```

Question
========
What is the value of `a*b`, given this 2 vectors? 

$$a =  `r a` $$
$$ b =  `r b`$$


Solution
========
The resulting vector is `r solution `.

Meta-information
================
exname: Arithmetic
extype: string
exsolution: `r solution`
extol: 0.01
